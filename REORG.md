# REORG — Анализ реорг-загрязнения realtime-диапазона

**Дата анализа:** 2026-07-16  
**Контекст:** ETH ERC-20 индексер, бинарь v21, БД `eth` (Scylla на 100.64.0.4)

---

## 1. Вывод

Всё расхождение между нашей БД и Dune (источник истины — canonical ETH chain) сконцентрировано
**исключительно** в realtime-индексированных блоках. Исторические данные (0 → 25,422,400) совпадают
с Dune с точностью до нуля.

Причина: realtime-цикл (`runRealtimeLoop` в `src/main.zig`) не обрабатывает реорги — каждый блок
индексируется ровно один раз в момент WSS-нотификации. Если блок впоследствии вытесняется из
canonical chain, данные в Scylla остаются от orphan-блока, а canonical-данные не попадают в БД.

---

## 2. Границы realtime-индексации

- **Первый realtime-блок:** `25,422,541`  
  (Из лога `eth_index_60.log`: первый `[rt] blk=25422541`, дата ~Jul 15 2026)
- **Snapshot-блок** для сравнения: `25,541,725` (зафиксирован 2026-07-16)

Блоки `25,422,401 – 25,422,540` (140 блоков) — переходная зона между историческим и realtime,
отдельно не проверялась (попадает в delta≈0 исторического диапазона, скорее всего чистые).

---

## 3. Данные по диапазонам

### 3.1 Исторические диапазоны — дельта = 0

| Диапазон | Блоков | BC TX | Dune TX | **ΔTX** | BC log | Dune log | **Δlog** |
|----------|--------|-------|---------|---------|--------|----------|---------|
| 0 → 25,000,000 | 25,000,001 | 3,438,274,822 | 3,438,274,822 | **0** | 6,706,403,606 | 6,706,403,606 | **0** |
| 25,000,001 → 25,422,400 | 422,400 | 126,121,916 | 126,121,916 | **0** | 346,932,271 | 346,932,271 | **0** |

**Итого исторически верифицированный диапазон: блоки 0 → 25,422,400 (25,422,401 блок)**  
Данные гарантированно чистые — perfect match с Dune.

### 3.2 Realtime-диапазон — ВСЯ дельта здесь

| Диапазон | Блоков | BC TX | Dune TX | **ΔTX** | BC log | Dune log | **Δlog** |
|----------|--------|-------|---------|---------|--------|----------|---------|
| 25,422,541 → 25,541,725 | 119,185 | 41,222,222 | 41,222,386 | **+164** | 92,459,086 | 92,459,753 | **+667** |

Дельта: 164 транзакции и 667 логов на 119,185 блоков = ~0.0004% TX, ~0.0007% log.

Вероятно, 1–3 orphan-блока: типичный реорг в PoS = 1 блок глубиной, 1–2 раза за этот период.
164 TX / среднее ~200 TX/блок ≈ примерно 1 block worth, что соответствует 1 реоргу.

---

## 4. Корень проблемы

```zig
// src/main.zig:288
if (blockNum <= cursorPos) continue;
```

Если canonical head опускается ниже cursorPos (реорг) — нотификация игнорируется.  
Уже записанные orphan-данные остаются в Scylla. Canonical-блок с тем же номером не переиндексируется.

---

## 5. Что нужно сделать

### 5.1 Починить существующие данные (119,185 блоков, realtime-диапазон)

Найти конкретные orphan-блоки:
- Инструмент: `sum_bc_totals` умеет SUM, но не per-block diff
- Нужен новый tool: для каждого блока в диапазоне 25,422,541→25,541,725 —
  сравнить `BC.tx_count` vs `eth_getBlockTransactionCountByNumber` (RPC) или Dune per-block
- Блоки с расхождением — удалить их данные из Scylla и переиндексировать

Альтернатива: переиндексировать весь диапазон `25,422,541→25,541,725` в историческом режиме
(с уже settled данными из Erigon — реорги теперь неактуальны для этих блоков).

### 5.2 Предотвратить в будущем: safety lag

Самое простое и надёжное: индексировать только блоки `>= HEAD - N` (safety lag N=64 = ~12 минут).
Блоки старше 64 от текущего head в PoS финализированы (2 эпохи = 64 блока — финальность Casper).

Реализация в `runRealtimeLoop`:
```zig
const lag = 64; // 2 Casper epochs ~ 12.8 min — финализированы
if (blockNum <= cursorPos + lag) continue; // ждём пока blockNum уйдёт на lag вперёд
// ...или: индексировать только blockNum - lag
```

Альтернатива: reorg detection — сравнивать `blockHash` через N блоков, при несовпадении удалять
и переиндексировать. Сложнее, но даёт минимальную latency.

---

## 6. Статус

| Задача | Статус |
|--------|--------|
| Локализовать дельту по диапазонам | ✅ DONE (2026-07-16) |
| Исторические данные 0→25,422,400 верифицированы | ✅ DONE — Δ=0 |
| Найти конкретные orphan-блоки в realtime-диапазоне | ⬜ TODO |
| Переиндексировать затронутые блоки | ⬜ TODO |
| Implement safety lag в realtime-цикле | ⬜ TODO |

---

## 7. Трейсы (ITX) — отдельная картина

В отличие от TX и logs, ITX-расхождение **не локализовано** в realtime-диапазоне.
Оно присутствует и в историческом диапазоне, со **сменой знака** по эрам:

| Диапазон | BC ITX | Dune adj | **Δ (Dune−BC)** | Тип |
|----------|--------|----------|-----------------|-----|
| 0 → 25,000,000 | 16,123,702,620 | 16,123,237,704¹ | **−464,916 (BC больше)** | PoW + ранний PoS |
| 25M → 25,422,400 | 881,673,596 | 881,813,910 | **+140,314 (Dune больше)** | PoS исторический |
| gap (140 блоков) | 267,624 | 267,631 | **+7 (≈0)** | PoS переходный |
| 25,422,541 → 25,541,725 | 252,013,007 | 252,026,077 | **+13,070 (Dune больше)** | PoS realtime |
| **Итого** | **17,257,656,847** | **17,257,345,322** | **−311,525 (BC больше)** | — |

¹ Вычислено как: full_dune − (25M→25.42M) − gap − realtime

**Вывод:** ITX-дельта — структурная разница reth vs Dune/geth/Erigon trace emission:
- В PoW-доминируемой зоне (0→25M): reth трейсит больше внутренних вызовов, чем geth
- В PoS-зоне (25M+): Dune трейсит чуть больше нас

Это **не** проблема целостности данных и **не** реорги — характер расхождения одинаков в
исторических и realtime-блоках. Подробнее: `DUNE_CHECK.md §5.3`.

---

## 8. Dune Query IDs

- TX 0→25M: `7996323`
- Log 0→25M: `7996338`
- TX 25M→25,422,400: `7996576`
- Log 25M→25,422,400: `7996580`
- TX+Log 25,422,541→25,541,725: `7996584`
- ITX 25M→25,422,400 (adj): `7996658`
- ITX 25,422,541→25,541,725 (adj): `7996661`
- ITX gap 25,422,401→25,422,540 (adj): `7996670`
